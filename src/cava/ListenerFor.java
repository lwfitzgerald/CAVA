package cava;

public enum ListenerFor {
	Artist,Album,Track,Playlist,SearchBox,CancelButton,Play,Pause,Stop,SkipFoward,SkipBack,newPlaylist,newSmartPlaylist,addToPlaylist, RemoveFromPlaylist, PlaylistTrack,seekBar, deletePlaylist, renamePlaylist, QuitProg;
}
